// Options for preformance and graphics quality. Adjust these to your liking. I will add a settings menu in the future.

export const resolutionScale = 1;
export const chunkResolution = 10; // terrain samples per chunk (16 is default so i dont forget)
export const RENDER_DIST = 8; // in chunks (radius)